<!-- <!DOCTYPE html>
<html lang="en">
<head>
</head>
<body class="bg-green-100">
    <div class="grid grid-cols-4">
        <div class="col-start-2 col-span-2">
            <p class="text-xl flex items-center">
                <a href="/sport" class="hover:underline">Sports</a> / <%= sport.name %> / Edit Session
            </p>
            <h2>Edit Session</h2>
            <form action="/sport/<%= sportId %>/sessions/<%= session.id %>/edit" method="POST">
                <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                <div>
                    <label for="sessionDateTime">Date and Time:</label>
                    <input type="datetime-local" id="sessionDateTime" name="sessionDateTime" value="<%= session.sessionDateTime %>" required>
                </div>
                <div>
                    <label for="sessionVenue">Venue:</label>
                    <input type="text" id="sessionVenue" name="sessionVenue" value="<%= session.sessionVenue %>" required>
                </div>
                <div>
                    <label for="sessionParticipants">Participants:</label>
                    <input type="text" id="sessionParticipants" name="sessionParticipants" value="<%= session.sessionParticipants %>" required>
                </div>
                <div>
                    <label for="playersNeeded">Players Needed:</label>
                    <input type="number" id="playersNeeded" name="playersNeeded" value="<%= session.playersNeeded %>" required>
                </div>
                <button type="submit">Update Session</button>
            </form>

            <h3>Members:</h3>
            <% session.Users.forEach(function(user) { %>
                <div>
                    <span><%= user.firstName %> <%= user.lastName %></span>
                    <form action="/sport/<%= sportId %>/sessions/<%= session.id %>/members/<%= user.id %>/edit" method="POST" style="display: inline;">
                        <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                        <button type="submit">Edit</button>
                    </form>
                    <form action="/sport/<%= sportId %>/sessions/<%= session.id %>/members/<%= user.id %>/delete" method="POST" style="display: inline;">
                        <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                        <button type="submit">Delete</button>
                    </form>
                </div>
            <% }); %>
        </div>
    </div>
</body>
</html> -->
